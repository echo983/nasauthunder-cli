{
  "name": "nasauthunder-cli",
  "version": "0.1.0",
  "description": "Archive local directories to Usenet using the nasauthunder immutable object protocol",
  "license": "MIT",
  "type": "module",
  "bin": { "nasauthunder": "dist/src/cli.js" },
  "files": ["dist/src", "README.md", "LICENSE", "config.example.json"],
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/**/*.test.js",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "7.0.2"
  }
}
